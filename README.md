# minerU Wrapper

PDF → structured Markdown parsing on ROCm GPU. Includes a FastAPI server for remote parsing.

## Quick Start

```bash
pip install httpx
python3 api_client.py paper.pdf http://<server>:<port>
```

See [mineru_wrapper.md](mineru_wrapper.md) for full usage.
